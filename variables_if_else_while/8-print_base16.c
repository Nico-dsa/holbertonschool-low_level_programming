#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int num;

	for (num = 0; num > 16; num--)
		puthcar ((num % 10) + '0')

	putchar ('\n')

	Return : (0);
}
